/**
 * Basic test for server.js
 * To run: npm test
 */

describe('Server', () => {
  test('should be truthy', () => {
    expect(true).toBe(true);
  });

  test('basic math', () => {
    expect(1 + 1).toBe(2);
  });
});
